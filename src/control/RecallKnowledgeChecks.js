import NPCModel from '../models/NPCModel';
import {adjustmentScaleMap} from "../constants/constants.js";
import {recallKnowledgeAssociatedSkills} from "../constants/constants.js";

/**
 *
 * @param npc = NPCModel
 * @param player
 * @param gmAdjustment
 */
function calculateRecallKnowledgeDC(npc, player, gmAdjustment = 0)
{

}